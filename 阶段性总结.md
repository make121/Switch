# 阶段性总结：在线技能调度器（Online Skill Scheduler）

> 日期：2026-08-06
> 对应设计文档：`switch_scheduler_spec.md`（Switch 论文 arXiv:2604.14834）
> 阶段：部署侧在线调度器已在 IsaacGym 评估环境闭环跑通（两技能 + 三技能）

---

## 1. 目标回顾

在 PBHC（KungfuBot2）运动跟踪框架上，实现 Switch 论文的部署期在线技能调度器：

```
按键/指令 → entry_check（attach/search/estop）→ 技能图搜索规划切换路径
         → 路径还原为参考轨迹注入运动库 → 策略跟踪 → 过渡完成进入目标技能循环播放
         → 失败时回当前技能起点重播（临时恢复机制）
```

调度器是纯图搜索 + 状态机模块，不参与训练、不依赖 RL。

## 2. 代码结构（全部为本次新增，除注明外）

```
SG_build/skill_graph_V2.py          # 【改】节点导出 kappa + q/q_dot/p_hat 特征；
                                    #     interpolate_frame 提为模块级函数
humanoidverse/deploy/__init__.py    # 【改】PEP 562 懒加载（torch 链与调度器解耦）
humanoidverse/eval_agent.py         # 【改】按键 7/8/9/0 → 技能 0/1/2/3；
                                    #     eval 配置保存失败不再崩溃

humanoidverse/deploy/skill_scheduler/
├── distance.py            # NodeState + sim()（分量归一化加权 L1，全项目唯一实现）
├── graph_data.py          # 运行时图：正/反邻接、部署边权（spec 1.2）、target_set
├── planners.py            # GraphSearchPlanner：反向多源 Dijkstra + V/next_hop 缓存
├── scheduler.py           # SkillGraphScheduler：entry_check 三分支 + step 四触发
├── reference_builder.py   # 路径 → 完整 PBHC 轨迹（buffer 帧按 κ 重算插值）
├── calibrate.py           # 离线标定：σ 统计、边类分布、A/B 候选（spec 6.1）
└── tests/
    ├── test_graph_search.py       # 11 项
    └── test_reference_builder.py  # 4 项

humanoidverse/agents/callbacks/skill_scheduler_callback.py
                           # eval 回调：状态提取、按键指令、轨迹热注入运动库、
                           # 参考生命周期管理（两段式）
```

产物：`SG_build/sg_output_V2/`（两技能图+标定）、`SG_build/sg_output_mm_deploy/`（三技能图+标定+技能 pkl）。

## 3. 已实现能力

| 能力 | 状态 | 说明 |
|---|---|---|
| sim() 统一距离 + 离线标定 | ✅ | spec 1.3 方案2；p_hat 取 z-only，部署侧 w_p=0 |
| 图搜索规划（Dijkstra） | ✅ | V/next_hop 按 T_cmd 缓存；不可达候选自动沉底+全局可达兜底 |
| 入口检查 attach/search/estop | ✅ | 候选池=全图节点，路径真正穿越跨技能边/buffer 链 |
| κ 透传 | ✅ | buffer 帧 κ=N−k+1 递减，接入 guidance |
| 按键技能切换 | ✅ | 7/8/9/0 → 技能 0/1/2/3，即时响应 |
| 参考注入 | ✅ | 替换 motion lib 条目 + load_motions 重载，obs/奖励零改动 |
| 平滑过渡 | ✅ | 近入口（best_sim≤A）免复位；远入口复位到路径起点（防错位秒杀） |
| 参考生命周期 | ✅ | 过渡完成→纯技能循环；摔倒/终止→回当前技能起点重播（临时恢复） |
| e-stop | ⚠️ 半成品 | 锁存+计数日志+新指令解锁/纯技能重生兜底；damping FSM 未实现 |
| 安全重规划 | ✅ 默认关闭 | `enable_safety_replan=False`（见 §4 坑 3） |
| 无头自动切换 | ✅ | `auto_switch_interval_steps` / `auto_switch_skills`，供标定用 |

## 4. 联调中发现并修复的关键问题（经验沉淀）

1. **跨技能边界参考瞬移**：建图时各技能独立做 x-y 居中/yaw 对齐，路径穿越切换边时参考幽灵全局跳变。→ `build_trajectory` 对 >0.3m 的逐帧 xy 跳变做重锚定。
2. **复位噪声是单向偏置**（`dof + rand×scale`），机器人出生即偏离参考 ~3.5 归一化单位 → init 时 estop 降级为 search + 1s 安全宽限期。
3. **安全重规划在跟踪期有害**（最大的坑）：姿态最近≠时间最近（马步类动作姿态周期相似），暂态超 B → 重规划把参考在时间上向前跳 ~18 帧 → 跟踪崩溃 → 复位死循环。→ 默认关闭，待恢复 FSM + A/B 标定后再启用。
4. **env 终止阈值比预期严格**（body_z 0.25m），远入口过渡后 0.5s 内会被秒杀 → F1 按入口距离决定是否物理复位。
5. **eval 必须加** `+env.config.enforce_randomize_motion_start_eval=False`，否则随机起点与"T_cmd=技能前 τ 帧"冲突。
6. **T_cmd 方向性**：时间边单向向前，落在目标区域之后的节点 V=∞，由打分沉底+可达兜底处理。
7. **三技能数据集内容重复**：mm 的 kick/punch 取自 HDM05 同 session 相邻 take（03_02_01 vs 03_02_02），逐帧最近邻仅 0.017-0.046 rad（参照组 0.24 rad），实质是同一套路。→ 需重新选 AMASS 序列（数据侧，未做）。

## 5. 验证记录

- 离线单元测试 15 项全过（图搜索正确性、路径合法性、κ 透传、xy 连续性、安全重规划默认关闭、cmd_change 兜底等）。
- 两技能（sg_augmented 教师策略 model_53000）交互实测：按键切换走图上过渡路径，无瞬移无主动复位。
- 三技能（mm_triple_skill model_190000）无头自动切换：7 次指令 7 次注入成功，无 estop 吞键；复位分类=良性 motion_end 循环 + 偶发 body_z。
- 终止原因已可观测（日志区分 time_out,motion_end / body_z / motion_far）。

## 6. 运行指令

两技能（horse_pose/horse_punch，B=5.0）：

```bash
python humanoidverse/eval_agent.py \
  +checkpoint=logs/MotionTracking/20260624_111638-sg_augmented-motion_tracking-g1_23dof_lock_wrist/model_53000.pt \
  +robot.motion.motion_file=example/motion_data/Horse-stance_pose.pkl \
  +env.config.enforce_randomize_motion_start_eval=False \
  +algo.config.eval_callbacks.skill_scheduler._target_=humanoidverse.agents.callbacks.skill_scheduler_callback.SkillSchedulerEvalCallback \
  +algo.config.eval_callbacks.skill_scheduler.config.skill_graph_path=SG_build/sg_output_V2/skill_graph.json \
  +algo.config.eval_callbacks.skill_scheduler.config.scheduler_config_path=SG_build/sg_output_V2/scheduler_config.yaml \
  "+algo.config.eval_callbacks.skill_scheduler.config.skill_pkl_files=[example/motion_data/Horse-stance_pose.pkl,example/motion_data/Horse-stance_punch.pkl]" \
  +algo.config.eval_callbacks.skill_scheduler.config.B=5.0
```

三技能（lie_down_stand_up/kick/punch，B=10.0，punch 起手 initial_skill=2）：

```bash
python humanoidverse/eval_agent.py \
  +checkpoint=logs/MotionTracking/20260714_203739-mm_triple_skill-motion_tracking-g1_23dof_lock_wrist/model_190000.pt \
  +robot.motion.motion_file=SG_build/sg_output_mm_deploy/skills/punch.pkl \
  +env.config.enforce_randomize_motion_start_eval=False \
  +algo.config.eval_callbacks.skill_scheduler._target_=humanoidverse.agents.callbacks.skill_scheduler_callback.SkillSchedulerEvalCallback \
  +algo.config.eval_callbacks.skill_scheduler.config.skill_graph_path=SG_build/sg_output_mm_deploy/skill_graph.json \
  +algo.config.eval_callbacks.skill_scheduler.config.scheduler_config_path=SG_build/sg_output_mm_deploy/scheduler_config.yaml \
  "+algo.config.eval_callbacks.skill_scheduler.config.skill_pkl_files=[SG_build/sg_output_mm_deploy/skills/lie_down_stand_up.pkl,SG_build/sg_output_mm_deploy/skills/kick.pkl,SG_build/sg_output_mm_deploy/skills/punch.pkl]" \
  +algo.config.eval_callbacks.skill_scheduler.config.B=10.0 \
  +algo.config.eval_callbacks.skill_scheduler.config.initial_skill=2
```

按键：`7/8/9/0` = 技能 0/1/2/3。常用回调参数：`A`、`B`、`tau`、`top_k`、`lambda_sw`、`lambda_cost`、`grace_s`、`candidate_pool`、`enable_safety_replan`、`w_qdot`、`initial_skill`、`auto_switch_*`。

## 7. 待办（按 spec 第 7 节顺序）

1. **NN planner**（spec 2.2）：最近邻 planner，与 Graph-Search 做 A/B 对比（接口已预留）。
2. **e-stop / damping 恢复状态机**（spec 4）：damping controller + is_stationary 判据 + 恢复规划（NN 两阶段 T_rec）。
3. **超参数网格标定**（spec 6.2）：Easy/Medium/Hard 用例 + SSR/NR 指标，用 `auto_switch` 做自动化；产出正式 `scheduler_config.yaml`。当前 A/B 均为启发式初值。
4. **buffer-aware imitation（训练侧）**：`is_buffer` 进 reward/obs，κ 被策略消费；`is_cross_skill_env` 已就位待接。
5. **MuJoCo sim2sim 接入**：urci.py 部署侧复用同一调度器包。
6. **三技能数据重制**：重选 HDM05 punch/kick 序列（避开同 session take）→ 重定向 → 重建图 → 必要时重训。
7. **遗留观察项 R3**：mm 策略 punch 起手段疑似不稳（init best_sim≈10），需纯播放基线确认后定论。

## 8. 当前未提交

所有改动在工作区未提交 git，建议尽快 commit 留快照。
